# Task 0: UI toolchain preflight

Initialize the approved Radix/shadcn Nova/Tailwind v4 toolchain in `apps/desktop` after checking the current shadcn CLI project information, documentation, and dry-run output. Preserve existing scripts and WebChucK Vite behavior. Add the `@/*` alias to TypeScript and Vite and use `@tailwindcss/vite`.

Acceptance:

- `components.json` describes a Radix, CSS-variable, Lucide project rooted at `apps/desktop`.
- Approved shadcn components are generated under `src/components/ui`.
- Existing frontend tests and Vite asset verification remain viable.
- No non-official registry sources are introduced.
